import React, { useRef } from "react";
import gsap from "gsap";
import { useGSAP } from "@gsap/react"; // ✨ 리액트 전용 훅
import { ScrollTrigger } from "gsap/ScrollTrigger";
import "../assets/styles/MainVisual.scss"; // 스타일은 아래에 있어요
